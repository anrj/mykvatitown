# Convoying leader — REAL HARDWARE.

lane_stop:
  stop_trigger_line: left
  at_line_follow_line: right
  min_line_frames: 10
  line_lost_frames: 4
  bottom_line_px: 25
  stop_hold_s: 1.0
  line_speed_mult: 0.35

control:
  loop_hz: 20
  accel_rate: 0.05
